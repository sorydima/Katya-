# Web Platform Documentation

This directory contains platform-specific documentation for Web builds and deployments.

## Build Instructions

- Run `flutter build web --release` for release builds.
- Use `flutter run -d chrome` for development.

## Requirements

- Modern web browser (Chrome, Firefox, Safari, Edge).
- Flutter SDK with web support enabled.

## Known Issues

- [List any Web-specific issues here]
