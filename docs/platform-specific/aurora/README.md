# Aurora Platform Documentation

This directory contains platform-specific documentation for Aurora OS builds and deployments.

## Build Instructions

- Run `flutter build aurora --release` for release builds.
- Use `flutter run -d aurora` for development.

## Requirements

- Aurora OS SDK.
- Flutter SDK with Aurora support.

## Known Issues

- [List any Aurora-specific issues here]
