import 'package:flutter/material.dart';

class IPWhitelistScreen extends StatelessWidget {
  const IPWhitelistScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('IP Whitelist'),
      ),
      body: const Center(
        child: Text('IP Whitelist Screen - Coming Soon'),
      ),
    );
  }
}
